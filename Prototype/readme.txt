math match requires python and pygame to run

python and pip can be installed at https://www.python.org/downloads/

you can install pygame through pip with 'pip install pygame'

once pygame is installed, MathMatch can be launched via 'python main.py' or just './main.py' in Unix systems